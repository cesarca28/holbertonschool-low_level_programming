#include <stdio.h>

/**
 * main - Imprime la valeur de 'n'
 *
 * Return: Always 0 (Success)
 */

int main()
{
	int n = 15;
	int A = 1;
	n = n + A;
	printf("La valeur de n est : %d\net de A : %d\n", n, A);
	return(0);
}